# NuttX built-in applications

`cboard/robot_main.cpp` implements the first NSH built-in command. Later `orb`, `param`, `sbus_input`, output and control commands will follow the same application boundary.
