# Reusable robot code

This directory contains the OS-independent C++ portions of the project: the uORB-style bus, parameters, robot configurations, actuators, `MixingOutput`, output interfaces and protocol codecs. All uORB message data structures are kept in the top-level `msg/` directory.
